import { InCollectionsPipe } from './in-collections.pipe';

describe('InCollectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new InCollectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
