import { RaidReportProfileUrlPipe } from './raid-report-profile-url.pipe';

describe('RaidReportProfileUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new RaidReportProfileUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
