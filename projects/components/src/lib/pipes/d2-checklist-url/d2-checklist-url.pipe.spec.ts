import { D2ChecklistUrlPipe } from './d2-checklist-url.pipe';

describe('D2ChecklistUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new D2ChecklistUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
