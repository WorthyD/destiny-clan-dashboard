import { MemberTypePipe } from './member-type.pipe';

describe('MemberTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MemberTypePipe();
    expect(pipe).toBeTruthy();
  });
});
