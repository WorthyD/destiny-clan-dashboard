import { DungeonReportUrlPipe } from './dungeon-report-url.pipe';

describe('DungeonReportUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new DungeonReportUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
