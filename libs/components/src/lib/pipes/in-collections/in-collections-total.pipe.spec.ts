import { InCollectionsTotalPipe } from './in-collections-total.pipe';

describe('InCollectionsTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new InCollectionsTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
