import { BungieDisplayNamePipe } from './bungie-display-name.pipe';

describe('BungieDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BungieDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
