import { ProfileUrlPipe } from './profile-url.pipe';

describe('ProfileUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
